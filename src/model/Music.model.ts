import { nanoid } from "nanoid";
import { type } from "os";

type Music = {
  id: string,
  name: string,
  author: string,
  genre: string,
  duration: string,  
  pic: string,
}


export default Music